# TECHNICAL DEBT REGISTER
# سجل الديون الفنية

| المعرف | العنوان | الحالة | الأثر | التاريخ | المرجع |
|---|---|---|---|---|---|
| **TECH-DEBT-001** | استخدام symlinks لـ LLVM 21 في postgres:16-alpine | مراقب ومثبت بالـ digest | High | 2026-08-18 | [ADR-015](../architecture/adr/ADR-015-pg-uuidv7-build-strategy-docker-alpine.md) |
| **TECH-DEBT-007** | BC01: Phone OTP و OAuth (Google/Apple/Facebook) غير مُنفَّذة في Backend | مفتوح — مصنف كـ Medium | Medium | 2026-08-19 | قرار راعي المشروع (2026-08-19) |
| **TECH-DEBT-008** | استضافة مسار جلب الملف الشخصي GET /users/me مؤقتاً تحت بادئة /auth | موثق ومعتمد — Low | Low | 2026-08-21 | [AF-PHASE2-CLOSURE-001](AF-PHASE2-CLOSURE.md) |
| **TECH-DEBT-009** | بقايا نمط «اختلاق البيانات عند الفشل» في 4 شاشات من لوحة التحكم | ✅ مُغلق — أُزيل النمط بالكامل (2026-08-22) | High | 2026-08-22 | [POLICY-SEC-001](#policy-sec-001-يُحظر-اختلاق-الحالة-أو-البيانات-في-مسارات-الفشل) |
| **TECH-DEBT-010** | `GET /admin/analytics/overview` لا يُصدر `totalFatwas` ولا `totalAudioSeries` | مفتوح — اللوحة تعرض «—» بدلاً من رقم مختلق | Medium | 2026-08-22 | [تفاصيل](#تفاصيل-tech-debt-010) |
| **TECH-DEBT-011** | نبذة الشيخ وصورته في اللوحة تُحفظ في المتصفح فقط، بلا أي مسار خادم | مفتوح — لا يوجد `PATCH` لبيانات صاحب المجلس | Medium | 2026-08-22 | [تفاصيل](#تفاصيل-tech-debt-011) |
| **TECH-DEBT-012** | لا توجد حماية خادمية تمنع تجميد آخر حساب SuperAdmin | مفتوح — الحماية حالياً في الواجهة فقط (منع تجميد الذات) | Medium | 2026-08-22 | [تفاصيل](#تفاصيل-tech-debt-012) |

---

## POLICY-SEC-001: يُحظر اختلاق الحالة أو البيانات في مسارات الفشل

* **التصنيف**: 🔴 قاعدة حوكمة إلزامية (Non-negotiable Engineering Policy).
* **تاريخ الاعتماد**: 2026-08-22.
* **النطاق**: كل الشيفرة — Backend (NestJS)، Flutter، Admin (React)، والسكربتات.

### 1. نص القاعدة

> **يُحظر منعاً تاماً أن ينتج أي مسار فشل (`catch` / `onError` / `else` احتياطي / timeout)
> حالةً أو بيانات أو جلسةً أو رسالة نجاح لم يُصدرها الخادم فعلاً.**
> كل فشل يجب أن يظهر للمستخدم كخطأ عربي واضح، وأن يترك النظام في حالته الحقيقية.

يشمل الحظر تحديداً:

1. **اختلاق جلسة أو هوية**: إنشاء `Authenticated` / `session` / `token` / دور (`role`) داخل `catch`.
2. **اختلاق بيانات**: إرجاع «بيانات تجريبية عند عدم الاتصال» (`Fallback sample data if offline`) من دالة تجلب بيانات حقيقية.
3. **اختلاق نجاح**: إظهار `message.success` أو SnackBar نجاح داخل معالج خطأ، أو تعديل الحالة المحلية (مثل `isRead = true`) بعد فشل الطلب.
4. **اختلاق جهوزية**: ضبط علامة مثل `_isInitialized = true` أو `status: 'ok'` بعد فشل التهيئة أو فشل فحص الصحة.
5. **كتم الخطأ**: `catch (_) {}` فارغ في مسار له أثر على ما يراه المستخدم أو على قرار أمني.

### 2. سبب اعتماد القاعدة (سابقتان حقيقيتان في هذا المشروع)

| # | الحادثة | الأثر الأمني | الإصلاح |
|---|---|---|---|
| 1 | `AuthNotifier.loginAsGuest()` في Flutter كان يُنشئ جلسة `Authenticated` بدور `STUDENT` غير الموجود أصلاً في الأدوار الخمسة المعتمدة، بلا أي تحقق. | تجاوز كامل للمصادقة من شاشة الدخول. | commit `33f1837` |
| 2 | `LoginScreen` في لوحة التحكم كان يُنشئ — في `catch` أي فشل غير مُصنَّف (انقطاع شبكة، 5xx، CORS، الخادم متوقف) — جلسة **SuperAdmin** محلية بالرمز الثابت `preview-mock-token-jwt`. | صلاحيات SuperAdmin كاملة بلا مصادقة لأي شخص يتسبب في خطأ اتصال. | commit `af9aada` |

الدرس المشترك: **مسار الفشل هو مسار أمني.** أي «تسهيل تجربة» يُنشئ حالة غير موجودة على الخادم
يتحول تلقائياً إلى ثغرة أو إلى إخفاء لانقطاع حقيقي في الخدمة.

### 3. التطبيق الإلزامي على كل شيفرة جديدة

* **Flutter**: استخدم `AppException.fromDioException()` في `core/network/error_handler.dart` ثم اعرض
  `AppException.message` في الواجهة (حالة خطأ + زر «إعادة المحاولة»).
* **Admin**: استخدم `toArabicErrorMessage()` في `core/api/errorMessage.ts` مع `Alert` أو `message.error`،
  ولا تضع `catch` داخل `queryFn` — اترك TanStack Query يُصدر `isError` وتعامل معه في الواجهة.
* **مرجع قياسي يُحتذى به**: `apps/admin/src/features/content/ContentListScreen.tsx` (لا `catch` في الجلب،
  و`message.error` عند فشل الحذف).
* **مراجعة الشيفرة**: أي `catch` يُرجع كائناً حرفياً (literal) أو يستدعي `success` يُرفض في الـ Code Review.
* **اختبار انحدار إلزامي**: `apps/admin/src/test/failure_paths.spec.tsx` — يتحقق لكل شاشة أن نص الفشل
  العربي ظاهر **وأن** النصوص والأرقام التي كانت تُنتجها الاحتياطيات القديمة غائبة تماماً. أي عودة
  للنمط تُسقط هذه الاختبارات. أضف حالة جديدة فيه مع كل شاشة تجلب بيانات.

---

## تفاصيل TECH-DEBT-009

* **العنوان**: بقايا نمط «اختلاق البيانات عند الفشل» في 4 شاشات من لوحة التحكم (Admin).
* **التصنيف**: 🔴 High (تُظهر بيانات وهمية للمشرفين وتُخفي انقطاع الخدمة، لكنها لا تمنح صلاحيات).
* **تاريخ التسجيل**: 2026-08-22.
* **الحالة**: ✅ **مُغلق في 2026-08-22** — بقرار راعي المشروع نُفِّذت الإزالة الكاملة في نفس الجلسة،
  مع اختبارات انحدار في `apps/admin/src/test/failure_paths.spec.tsx`.

### 1. الحصر الدقيق للمواضع (10 مواضع في 4 ملفات) وحالة كل منها

| الملف | الموضع | الوصف | الحالة |
|---|---|---|---|
| `dashboard/DashboardScreen.tsx` | `queryFn` الإحصاءات | `catch` يُرجع إحصاءات مختلقة (86 مادة، 1420 مستخدماً، 48920 مشاهدة، و4 صفوف محتوى وهمية) | ✅ أُزيل |
| `dashboard/DashboardScreen.tsx` | `queryFn` فحص الصحة | `catch` يُرجع `{ status: 'ok', database: 'up' }` — **يُظهر النظام سليماً عند فشل فحص الصحة فعلياً** | ✅ أُزيل |
| `announcements/AnnouncementsScreen.tsx` | `queryFn` | `catch` يُرجع قائمة إعلانات مختلقة | ✅ أُزيل |
| `announcements/AnnouncementsScreen.tsx` | `onError` ×3 | ثلاث رسائل `message.success` داخل `onError` (بث / حذف / تعديل إعلان) | ✅ حُوِّلت إلى `message.error` |
| `content/CategoriesScreen.tsx` | `queryFn` | `catch` يُرجع 5 أقسام مختلقة مع `contentCount` وهمي | ✅ أُزيل |
| `content/CategoriesScreen.tsx` | `onError` ×2 | رسالتا `message.success` داخل `onError` (إضافة / حذف قسم) | ✅ حُوِّلت إلى `message.error` |
| `content/ContentModal.tsx` | `queryFn` | `catch` يُرجع 5 أقسام مختلقة في قائمة اختيار القسم | ✅ أُزيل |

### 2. مواضع إضافية اكتُشفت أثناء التنفيذ (لم تكن في الحصر الأصلي)

حذف الـ `catch` وحده **لم يكن كافياً** في `DashboardScreen`: القيم المختلقة كانت تُعاد إنتاجها
عبر قيم افتراضية في طبقة العرض:

| الموضع | المشكلة | المعالجة |
|---|---|---|
| بطاقات `Statistic` ×4 | `\|\| 86` / `\|\| 48920` / `\|\| 1420` / `\|\| 5` تُنتج الأرقام المختلقة نفسها عند الفشل | استُبدلت بـ `?? '—'` |
| زر «تحديث» | `message.success` غير مشروط بعد `refetch()` مهما كانت النتيجة | يفحص `isError` لكل من الطلبين ويُظهر الخطأ الحقيقي |
| بطاقة الشيخ | ثلاثية ثابتة (86 مادة / 42 فتوى / 24 سلسلة صوتية) | تقرأ من الخادم، و«—» لما لا يُصدره الخادم (TECH-DEBT-010) |
| `+18% هذا الشهر` | نسبة نمو مختلقة لا يُصدرها الخادم | أُزيلت واستُبدلت بوسم «من الخادم» |
| نافذة نبذة الشيخ | `message.success('تم تحديث بيانات الشيخ')` دون أي استدعاء API | تحذير صريح بأن الحفظ محلي فقط (TECH-DEBT-011) |
| جدول أكثر المواد | `emptyText` واحد لا يفرّق بين «لا توجد بيانات» و«فشل الجلب» | نصّان مختلفان |

### 3. خطر بياني كامن أُصلح ضمنياً

`ContentModal` و`CategoriesScreen` يتشاركان نفس `queryKeys.content.categories()` لكن بشكل
بيانات مختلف: نسخة `ContentModal` لم تكن تُخرج `contentCount`. فمن يُحمَّل أولاً يملأ الذاكرة
المؤقتة للآخر — ولو سبق `ContentModal`، لرأت شاشة الأقسام `contentCount = 0` في كل صف،
فيسلك حذف قسم **غير فارغ** مسار `DIRECT` بلا إعادة تصنيف لمحتوياته. وُحِّد الشكل في الاثنين.

### 4. أثر مستقل: `isSelf` كان يقارن الدور لا الهوية

في `UserListScreen.tsx` كان الشرط `record.role === 'SuperAdmin' && currentAdminRole === 'SuperAdmin'`،
أي أنه:

* يُخفي زر التجميد عن **كل** صفوف الـ SuperAdmin أمام أي SuperAdmin (منع إجراءات مشروعة)، و
* لا يحمي حساب الـ `Admin` من تجميد نفسه إطلاقاً (الشرط لا يتحقق أبداً له).

صار المقارنة بالمعرّف: `record.id === currentAdmin?.id`. الحماية الخادمية لآخر SuperAdmin
مسجَّلة في TECH-DEBT-012.

---

## تفاصيل TECH-DEBT-010

* **العنوان**: `GET /admin/analytics/overview` لا يُصدر `totalFatwas` ولا `totalAudioSeries`.
* **التصنيف**: 🟡 Medium. **تاريخ التسجيل**: 2026-08-22. **الحالة**: مفتوح.
* **السياق**: بطاقة «عن صاحب المجلس» في اللوحة تعرض ثلاثة أرقام. الأول (`totalContent`) يأتي من
  الخادم، والاثنان الآخران لم يكن لهما مصدر فعلي وكانا ثابتين (42 و24).
* **الوضع الحالي**: تُعرض «—» بدلاً منهما — التزاماً بـ POLICY-SEC-001، لأن رقماً مختلقاً في تقرير
  إداري أسوأ من غياب الرقم.
* **الإصلاح المطلوب**: إضافة الحقلين إلى مُخرَج `AnalyticsService.getOverview()`
  (عدّ المحتوى بـ `type = TEXT` مع تصنيف الفتاوى، وعدّ `type = AUDIO`)، ثم استبدال «—» في
  `DashboardScreen.tsx` بالقراءة منهما. لا حاجة لأي تغيير في العقود.

---

## تفاصيل TECH-DEBT-011

* **العنوان**: نبذة الشيخ وصورته تُحفظان في المتصفح فقط، بلا مسار خادم.
* **التصنيف**: 🟡 Medium. **تاريخ التسجيل**: 2026-08-22. **الحالة**: مفتوح.
* **السياق**: نافذة تعديل النبذة في اللوحة تكتب في `cms.store` (Zustand + `localStorage`) ولا
  تستدعي أي API. كانت تُظهر «تم تحديث بيانات الشيخ» — وهي رسالة نجاح لعملية لم تحدث على الخادم.
  كما أن رابط الصورة يُقرأ عبر `URL.createObjectURL` فلا يبقى بعد إعادة التحميل.
* **الوضع الحالي**: تحذير صريح للمشرف بأن الحفظ محلي في هذا المتصفح فقط وأن رابط الصورة لا يبقى.
* **الإصلاح المطلوب**: مسار `PATCH /admin/site-settings` (أو ما يعادله) لبيانات صاحب المجلس،
  ورفع الصورة إلى التخزين الدائم بدل `ObjectURL`، ثم ربط النافذة به.

---

## تفاصيل TECH-DEBT-012

* **العنوان**: لا توجد حماية خادمية تمنع تجميد آخر حساب SuperAdmin.
* **التصنيف**: 🟡 Medium. **تاريخ التسجيل**: 2026-08-22. **الحالة**: مفتوح.
* **السياق**: بعد إصلاح `isSelf` صار المشرف محمياً من تجميد **حسابه هو** في الواجهة. لكن
  SuperAdmin «أ» ما زال قادراً على تجميد SuperAdmin «ب»، وإن كان الأخير هو الوحيد المتبقي
  فقد يفقد النظام كل صلاحياته الإدارية العليا.
* **الإصلاح المطلوب**: تحقق في `AdminUsersService.suspend()` يرفض العملية إذا كان الهدف آخر
  `SuperAdmin` نشط (أو إذا كان الهدف هو المنفِّذ نفسه) — الحماية يجب أن تكون في الخادم لأن
  الواجهة ليست حدوداً أمنية.

---

## تفاصيل TECH-DEBT-007

* **العنوان**: BC01: Phone OTP و OAuth (Google/Apple/Facebook) غير مُنفَّذة في Backend رغم وجود الجداول والعقود الموثقة — Controllers/Services/DTOs غير مبنية بعد.
* **التصنيف**: 🟡 Medium
* **تاريخ التسجيل**: 2026-08-19
* **المسؤول**: فريق الـ Backend

### 1. الوصف والسياق
* **الجداول في قاعدة البيانات**: جدول `id_social_identities` وجدول `id_otp_challenges` موجودان ومُنشآن بنجاح عبر `Migration 004` (`004_identity_oauth_otp.sql`).
* **عقد الـ API**: موثّق بالكامل في `API-DESIGN.md` (`POST /api/v1/auth/register/phone/initiate`, `POST /api/v1/auth/register/phone/verify`, `POST /api/v1/auth/login/phone/initiate`, `POST /api/v1/auth/login/phone/verify`, `POST /api/v1/auth/login/google`, `POST /api/v1/auth/login/apple`, `POST /api/v1/auth/login/facebook`).
* **فجوة التنفيذ في NestJS**:
  * لا توجد TypeORM Entities (`SocialIdentityOrmEntity`, `OtpChallengeOrmEntity`).
  * لا توجد DTOs أو Services لطلب والتحقق من الـ OTP أو معالجة رموز OAuth.
  * `auth.controller.ts` يحتوي فقط على 6 مسارات خاصة بـ Email Auth (`registerEmail`, `loginEmail`, `refreshToken`, `logout`, `getMe`, `changePassword`).
  * الـ 24 اختبار تكامل لـ BC01 تغطي مسار Email Auth فقط.

### 2. القرار المعماري المؤقت (Sponsor Decision 2026-08-19)
* متابعة بناء واجهات تطبيق Flutter (Phase 2) مع تفعيل **Email + Password** فقط في MVP الحالي.
* إظهار أزرار Phone OTP و Google/Apple/Facebook في واجهة الدخول بحالة معطلة صراحة ("قريباً" / Coming Soon) لتجهيز البنية التفاعلية دون كسر الـ UX.
* جدولة استكمال كود الـ Backend واختبارات التكامل لـ OTP و OAuth في سباق مخصص لاحقاً.

---

## تفاصيل TECH-DEBT-008

* **العنوان**: مسار الملف الشخصي `GET /users/me` مستضاف مؤقتاً تحت بادئة `/auth` كـ `GET /api/v1/auth/users/me` داخل `AuthController`.
* **التصنيف**: 🟢 Low (Architectural Routing Debt).
* **تاريخ التسجيل**: 2026-08-21.
* **المسؤول**: فريق الـ Backend + فريق الـ Mobile.

### 1. الوصف والسياق
* تم وضع مسار جلب الملف الشخصي داخل `AuthController` في المرحلة الأولى لإثبات وتأكيد عمل `JwtAuthGuard` و `JwtStrategy` مع توقيعات RS256.
* لأن بادئة الكنترولر هي `@Controller('auth')`، أصبح المسار الفعلي الصادر هو `/api/v1/auth/users/me`، وهو مختبر وموثق في `API-DESIGN.md` وفي اختبارات تكامل BC01 الأصلية الـ 24.

### 2. خطة التفكيك والاستخراج
* عند بناء باقي مسارات إدارة الحساب في مراحل لاحقة (`PATCH /users/me`, `DELETE /users/me`, `GET/DELETE /users/me/sessions`)، سيتم استخراج `UsersController` مستقل تحت بادئة `/users` بكل نظافة وسلاسة دون أي تأثير على منطق الـ Domain والـ Repositories.

